
? Auto-creating of XMPP users should probably be handled over login_required
 (since it gets the 'request' object), with options like "show registering
 offer", "warn about registration and offer to set the name" and
 "automatically register and use JID as username"

TODO: make XMPP XHTML templates use non-relative links.

WARN: can't use 'include' in ptf templates.

page-global pagination numeration:
  {{ page_obj.start_index|add:forloop.counter|add:-1 }}

imgtag_re = \
  re.compile(r'<img\s+(src=[\'"](?P<src>.*?)[\'"]|(?P<alt>alt=)|(?P<title>title=))[^>]*>');
m = imgtag_re.findall(t); m[0]
